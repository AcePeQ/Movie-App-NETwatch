import styles from "./TabInformations.module.css";

import MovieRating from "../../MovieRating/MovieRating";
import Select from "react-select";



function TabInformations({ movieData }) {


  const {
    first_air_date,
    name,
    title,
    release_date,
    runtime,
    seasons,
    status,
    title,
    vote_average,
    genres,
  } = movieData;

  return (

  );
}

export default TabInformations;
